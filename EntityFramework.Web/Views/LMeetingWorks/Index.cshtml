@model IEnumerable<TestEntityFramework.Models.LMeetingWork>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IsAbsent)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FMeetingNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FPersonNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IsAbsent)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FMeetingNavigation.FMeetingId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FPersonNavigation.FPersonId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LMeetingWorkId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LMeetingWorkId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LMeetingWorkId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
